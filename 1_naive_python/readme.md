# Naive Python Implementation

First, I gathered a naive python implementation to get a baseline of how quickly python could process the file. 

The code uses the built-in CSV reader to pass the file and using a simple for loop to store the data in a dictionary. The average being calculated at the end based on a council values and a total. This simple version had a runtime of seven minutes and 38 seconds.