# Naive Go

Forgo, I produced essentially the same code except rather than using a CSV library. I split the string on the separator character storing the data in a map in much the same way as you were a Dictionary.

This implementation took one minute and 19 seconds.